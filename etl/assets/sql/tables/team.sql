CREATE TABLE team (
    id INT PRIMARY KEY,
    conference VARCHAR(255),
    division VARCHAR(255),
    city VARCHAR(255),
    name VARCHAR(255),
    "fullName" VARCHAR(255),
    abbreviation VARCHAR(5)
);